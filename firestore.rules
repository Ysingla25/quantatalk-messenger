rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all authenticated users for all documents
    // THIS IS TEMPORARY - ONLY FOR TESTING
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}